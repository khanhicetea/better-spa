# Public URL of the existing KitKit backend. Use HTTPS outside local development.
# For a physical device, use an address reachable from the device (not localhost).
EXPO_PUBLIC_API_URL=http://192.168.1.100:3000
